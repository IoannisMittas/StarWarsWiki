<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mittas.starwarswiki">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.list.character.CharacterListActivity" />
        <activity android:name=".ui.detail.CharacterDetailActivity" />
        <activity android:name=".ui.list.favourite.FavouriteCharacterListActivity"></activity>
    </application>

</manifest>